---
title: Setting up a matrix bot
date: 2026-03-07 00:00:00 +0800
categories: [General]
tags: [matrix]
description: guide to setting up a matrix bot
toc: true
---

## Introduction
This post serves to guide users on how to set up a matrix bot. (<https://docs.mau.fi/maubot/usage/setup/index.html>)

## Prequisites
Firstly, you need python to be above version 3.10. Secondly, you will need a matrix account to run your bot on.

## Creating a folder
Next, you will create a folder to store your configuration files and database for your matrix bot or what i call, maubot.

## Creating a virtual environment
Afterwards, create a virtual environment in this folder.

## Installing maubot in your virtual environment
Install maubot via this command:

```bash
pip install maubot
```
## Creating the required folders
Create the required folder for plugins,logging and storing trash via this command:

```bash
mkdir plugins trash logs
```

## Creating config.yaml
Create a file called config.yaml and paste the below content into it:

```bash
# Bot account details
user:
    credentials:
        id: "@bot:example.com"
        homeserver: https://example.com
        access_token: foo
        # If you want to enable encryption, set the device ID corresponding to the access token here.
        # When using an appservice, you should use appservice login manually to generate a device ID and access token.
        device_id: null
    # Enable /sync? This is not needed for purely unencrypted webhook-based bots, but is necessary in most other cases.
    sync: true
    # Receive appservice transactions? This will add a /_matrix/app/v1/transactions endpoint on
    # the HTTP server configured below. The base_path will not be applied for the /transactions path.
    appservice: false
    # When appservice mode is enabled, the hs_token for the appservice.
    hs_token: null
    # Automatically accept invites?
    autojoin: false
    # The displayname and avatar URL to set for the bot on startup.
    # Set to "disable" to not change the the current displayname/avatar.
    displayname: Standalone Bot
    avatar_url: mxc://maunium.net/AKwRzQkTbggfVZGEqexbYLIO

    # Should events from the initial sync be ignored? This should usually always be true.
    ignore_initial_sync: true
    # Should events from the first sync after starting be ignored? This can be set to false
    # if you want the bot to handle messages that were sent while the bot was down.
    ignore_first_sync: true

# Web server settings. These will only take effect if the plugin requests it using `webapp: true` in the meta file,
# or if user -> appservice is set to true.
server:
    # The IP and port to listen to.
    hostname: 0.0.0.0
    port: 8080
    # The base path where the plugin's web resources will be served. Unlike the normal mode,
    # the webserver is dedicated for a single bot in standalone mode, so the default path
    # is just /. If you want to emulate normal mode, set this to /_matrix/maubot/plugin/something
    base_path: /
    # The public URL where the resources are available. The base path is automatically appended to this.
    public_url: https://example.com

# The database for the plugin. Used for plugin data, the sync token and e2ee data (if enabled).
# SQLite and Postgres are supported.
database: sqlite:bot.db
crypto_db_pickle_key: mau.crypto

# Additional arguments for asyncpg.create_pool() or sqlite3.connect()
# https://magicstack.github.io/asyncpg/current/api/index.html#asyncpg.pool.create_pool
# https://docs.python.org/3/library/sqlite3.html#sqlite3.connect
# For sqlite, min_size is used as the connection thread pool size and max_size is ignored.
database_opts:
    min_size: 1
    max_size: 10

# Config for the plugin. Refer to the plugin's base-config.yaml to find what (if anything) to put here.
plugin_config: {}

# Standard Python logging configuration
logging:
    version: 1
    formatters:
        colored:
            (): maubot.lib.color_log.ColorFormatter
            format: "[%(asctime)s] [%(levelname)s@%(name)s] %(message)s"
    handlers:
        console:
            class: logging.StreamHandler
            formatter: colored
    loggers:
        maubot:
            level: DEBUG
        mau:
            level: DEBUG
        aiohttp:
            level: INFO
    root:
        level: DEBUG
        handlers: [console]

```

## Editing config.yaml
For the bot credentials, we will be using localhost as we are hosting it ourselves

Your access token can be retrieved via clicking on your profile picture on the top left > All settings > Help & about > scroll down > Acess token > copy it


```bash
credentials:
        id: "@example:matrix.org"
        homeserver: https://localhost:8080
        access_token: foo
        # If you want to enable encryption, set the device ID corresponding to the access token here.
        # When using an appservice, you should use appservice login manually to generate a device ID and access token.
        device_id: null

```

Afterwards, run maubot one time for it to fully generate config.yaml. 

```bash
python3 -m maubot
```

Config.yaml should now have a section called admins where you can create an admin user:

```bash
admins:
    root: ''
    admin: 'admin'
```

## Creating a client on maubot manager
Go to http://localhost:8080/_matrix/maubot/#/login, enter the credentials of the admin user you just created and log in. You will see this page:

![screenshot of maubot page](assets/posts/post7/post7_img1_maubotmanager.png)

Click on clients and fill out the details of your bot:

![screenshot of maubot manager create clients page](assets/posts/post7/post7_img2_client.png)

If successful, you will see the profle picture of the matrix account used.

![screenshot of working client](assets/posts/post7/post7_img3_workingclient.png)

## Uploading a plugin on maubot manager
You will have to first download a plugin which is in a .mbp format. I will be using coffee's animemanga plugin here. (<https://coffeebank.github.io/coffee-maubot/animemanga/>). After downloading it go back to your maubot manager page and click on 'plugins' and upload it there:

## Creating an instance on maubot manager
On maubot manager, click on 'instances'. Primary user will be the client you just created and type being the plugin you just created:

![screenshot of plugin](assets/posts/post7/post7_img5_instance.png)

## Results
Assuming you have invited the matrix account to your room, your bot will now work:

![screenshot of plugin](assets/posts/post7/post7_img6_results.png)
