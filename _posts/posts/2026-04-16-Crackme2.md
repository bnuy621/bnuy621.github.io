---
title: "Crackme: 69cd36983e328e778db052c2"
date: 2026-04-16 00:00:00 +0800
categories: [crackme]
tags: [misc]    
description: 
toc: true
---

## Introduction
My first attempt at doing one of these so im just gonna stick with the easy ones. <https://crackmes.one/crackme/69cd36983e328e778db052c2>. Upon looking at the file, i can tell it was compiled from assembly due to the `.out` extension.


## Files
1 file was given

1. main.out, VirusTotal: <https://www.virustotal.com/gui/file/ba7c61f81dbeb140cbcbfddbcf0eee2f0ab56118242c868ad8ee559333c116ff?nocache=1>


## Steps

### Analysis
I opened `main.out` in DIE to verify its filetype:

![DIE](assets/posts/post36/DIE.png)

As it is an ELF, i ran it in Kali to observe what it does.

![run failed](assets/posts/post36/img1_runfail.png)

When loading it into IDA,  we can see the flow of execution, this program was meant to fail as shown earlier.  I have to fix that.

![failed](assets/posts/post36/img2_IDA.png)

Firstly, i will decompile it. This is done by selecting `File` > `Produce file` > `.ASM file` on the top left of the window:

![failed](assets/posts/post36/img4_producefile.png)

Afterwards, i will replace this line with `NOP` which does nothing. This causes the code to NOT jump hence going directly to showing `success`. :

![failed](assets/posts/post36/img5_delete.png)

### recompiling it
This was my configuration. 
```bash
launch.json:

{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Launch(gdb)",
            "program": "${fileBasenameNoExtension}",
            "cwd": "${workspaceRoot}"
        }
    ]
}
```
tasks.json:
```bash
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Assemble and Link",
            "type": "shell",
            "command": "nasm -f elf64 -g -F dwarf ${file} -o ${fileBasenameNoExtension}.o && gcc -no-pie ${fileBasenameNoExtension}.o -o ${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared",
                "clear": true
            },
            "problemMatcher": []
        }
    ]
}
```

```bash
extern printf

section .data
format: db 'Hello in my first programm for crackme.one',0Ah,0
aSuccess: db 'Success!',0Ah,0
aError: db 'Error!',0

section .text
global main

main:
    endbr64
    push rbp
    mov rbp, rsp
    sub rsp, 16
    mov dword [rbp-4], 0
    lea rdi, [rel format]
    mov al, 0
    call printf
    mov dword [rbp-8], 0Ah
    cmp dword [rbp-8], 0Ah
    jne error_label
    lea rdi, [rel aSuccess]
    mov al, 0
    call printf
    jmp end_label
error_label:
    lea rdi, [rel aError]
    mov al, 0
    call printf
end_label:
    mov eax, [rbp-4]
    add rsp, 16
    pop rbp
    ret
```

### Results
Now recompile it and try running:

![failed](assets/posts/post36/img5_delete.png)

## Conclusion
To `fix` this binary, you have to replace the jump with `NOP` to get the code to display 'success`. I am unsure if recompiling it is considered cheating due to a change in the code.